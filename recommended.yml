server:
  port: 9092
  
eureka:
  client:
    healthcheck:
      enabled: true
    serviceUrl:
      defaultZone: http://localhost:9091/eureka/
    register-with-eureka: false
    fetch-registry: false
